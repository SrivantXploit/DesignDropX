const fs = require('fs'); let code = fs.readFileSync('script.js', 'utf8'); code = code.replace(/const fullHTML = \\\\\/g, 'const fullHTML = '); code = code.replace(/\\\\\r?\n/g, '\n'); code = code.replace(/<style>\\\\\<\/style>/g, '<style></style>'); code = code.replace(/\\\\\/g, ''); code = code.replace(/\\\\\/g, ''); code = code.replace(/\\\\\/g, ''); code = code.replace(/  \\\\\;/g, '  ;'); fs.writeFileSync('script.js', code);
